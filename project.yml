name: HoldSpeak
options:
  bundleIdPrefix: com.timmal
  deploymentTarget:
    macOS: "13.0"
  createIntermediateGroups: true

packages:
  GRDB:
    url: https://github.com/groue/GRDB.swift
    from: 6.0.0
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit
    from: 0.9.0

settings:
  base:
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    SWIFT_VERSION: "5.9"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ""
    ENABLE_HARDENED_RUNTIME: NO
  configs:
    Debug:
      CODE_SIGNING_REQUIRED: NO
      CODE_SIGNING_ALLOWED: YES
    Release:
      CODE_SIGNING_REQUIRED: NO
      CODE_SIGNING_ALLOWED: YES

targets:
  HoldSpeak:
    type: application
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: Sources/App
      - path: Sources/UI
      - path: Sources/Core
      - path: Sources/Whisper
        optional: true
      - path: Resources/radio.svg
        buildPhase: resources
      - path: Resources/settings.svg
        buildPhase: resources
      - path: Resources/AppIcon.icns
        buildPhase: resources
      - path: Resources/terminology-default-ru.json
        buildPhase: resources
      - path: Resources/terminology-default-uk.json
        buildPhase: resources
      - path: Resources/terminology-default-en.json
        buildPhase: resources
    dependencies:
      - package: GRDB
        product: GRDB
      - package: WhisperKit
        product: WhisperKit
    info:
      path: Resources/Info.plist
      properties:
        LSUIElement: true
        CFBundleDisplayName: HoldSpeak
        CFBundleName: HoldSpeak
        CFBundleIconFile: AppIcon
        CFBundleIconName: AppIcon
        CFBundleShortVersionString: "0.2.6"
        CFBundleVersion: "1"
        NSMicrophoneUsageDescription: HoldSpeak transcribes your speech locally while you hold the hotkey.
        NSHumanReadableCopyright: ""
    entitlements:
      path: Resources/HoldSpeak.entitlements
      properties:
        com.apple.security.app-sandbox: false
        com.apple.security.device.audio-input: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.timmal.push-to-talk
        MARKETING_VERSION: "0.2.6"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: NO
